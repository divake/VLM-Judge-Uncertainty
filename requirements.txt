# ── Core ML ──────────────────────────────────────────────────────────────────
torch>=2.0.0
transformers>=4.37.0          # needs VLM support (LLaVA, InternVL, Qwen-VL)
accelerate>=0.26.0
bitsandbytes>=0.41.0          # int4 NF4 quantization for large VLMs

# ── VLM-specific ──────────────────────────────────────────────────────────────
Pillow>=9.0.0                 # image loading
qwen_vl_utils                 # Qwen2.5-VL utility functions
timm                          # vision backbone (InternVL dependency)

# ── Conformal Prediction ──────────────────────────────────────────────────────
# R2CCP==0.0.8: install from wheel: pip install R2CCP-0.0.8-py3-none-any.whl --no-deps
mapie==0.8.6                  # CRITICAL: do NOT upgrade to v1.x (API breaking change)
pytorch_lightning>=2.0.0
configargparse
torchvision

# ── Data & Analysis ───────────────────────────────────────────────────────────
datasets>=2.14.0
pandas>=1.5.0
numpy>=1.24.0
scipy>=1.10.0
scikit-learn>=1.2.0

# ── Visualization ────────────────────────────────────────────────────────────
matplotlib>=3.7.0
seaborn>=0.12.0
jupyter

# ── Utilities ────────────────────────────────────────────────────────────────
tqdm
psutil
requests
